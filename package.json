{
  "name": "decimal-to-binary-api",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage",
    "test:ci": "jest --ci --reporters=default --reporters=jest-junit"
  },
  "jest-junit": {
    "outputDirectory": "reports",
    "outputName": "junit.xml"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.0.0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
